<ul class="nav nav-list" id="sideNav">
	{{#each item in view.navItems}}
		{{#view view.NavigationItemView }}
			{{#linkTo "database" item}}<i class="icon-chevron-right"></i>{{item.name}} {{item.collections.length}}{{/linkTo}}
		{{/view}}
	{{/each}}
</ul>
